# 裁库 v0.1.22

- 分析分类结果右侧预览改为完整 9:16 画幅，不再用横向小窗裁切竖屏素材。
- 预览统一使用原比例 `contain` 显示，不放大、不裁切，避免误判素材构图与清晰度。
- 新增窗口级全尺寸预览，保留播放、进度、静音和关闭操作。
- 大预览与右侧预览同步播放位置，关闭后返回原操作按钮，键盘操作更顺畅。
- 窄窗口下自动切换为上下布局，预览和片段编辑功能均可继续使用。
